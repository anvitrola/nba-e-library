import styled from 'styled-components'

export const HeaderContainer = styled.header`
    position: absolute;
    padding: 1.5rem;
    height: auto;
    z-index: 100;
    display: flex;

    background-color: transparent;

    align-items: center;
    justify-content: space-between;

`

export const NBALogo = styled.h1`
    img {
        width: 50px;
    }
`